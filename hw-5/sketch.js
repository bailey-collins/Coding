function setup() {
  createCanvas( windowWidth, 600 );
}

Let centerSquare = 0;

function draw() {
  background( 'rgb( 0, 128, 255)');

push();
  fill('rgb( 51, 153, 255)');
  square(30, 20, 100);
pop();
}
